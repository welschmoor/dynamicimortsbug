import ColorBlock from "@/components/ColorBlock";

export default function ColorPage() {
  return (
    <div>
      <ColorBlock color="#000000" text="You see us all at the same time" />
      <ColorBlock color="#c10404" text="You see us all at the same time" />
      <ColorBlock color="#e1dc49" text="You see us all at the same time" />
    </div>
  );
}
